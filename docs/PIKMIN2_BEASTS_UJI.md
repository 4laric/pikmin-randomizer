# Optional Hole of Beasts source-count Uji roster

Run `python -m experimental.pikmin2_beasts_uji` with `--assets`, `--assembly`, `--pod`, `--content-import`, `--uji-import`, and a new `--output` directory. This separate wrapper opts into the existing Uji visual/identity implementation without changing the basic cargo-only profile.

The source four UjiB and three separate two-UjiA definition rows become ten distinct instance IDs and native generator IDs61000–61009. Source corpse values remain1 for UjiA and2 for UjiB (14 total if every corpse is delivered). The source juji_key_fc cargo remains100 Pokos/5strength/10slots. Unsupported plants and descent remain explicitly unsupported.

The existing two-room engineering assembly is preserved. Four transformed type1 source candidates in the east room are selected for males. Three type0 groups use symmetric15-unit horizontal offsets within source radii/count limits for females. Original slot positions, angles, instance/source indices and engineering offsets remain in the manifest. All positions pass decoded ground checks. This is deterministic authored placement, not retail generation or a claim that the source selects these same candidates. The concentrated east-room distribution is an engineering playtest choice.

P1 KabekuiA/B AI, collision and attack timing remain proxies; source visuals are static sampled poses. No new production native code is included. Three focused tests cover counts/IDs/values, source count rejection, and ground rejection. Actual private preparation passed and decoded its emitted generator identities back for verification.

Prepared profile: `output/p2-beasts-uji-batch/runs/3004757e5a76497db9215c1df723dab7`. Launch a compatible fixed native executable there with `--experimental-pikmin2-room` and the usual MinGW runtime DLL path. Native runtime validation of this ten-actor roster is pending; the earlier two-actor fixture does not establish this profile's acceptance.
