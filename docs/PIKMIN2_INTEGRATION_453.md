# Integration sweep #453

Owner: Codex through shared account 4laric. Draft #432 baseline 02beaf5. Native remains 3213dd590de474331f67c2b65437e62d3c1b8236: this sweep changes root tools/docs/tests only.

## Integrated

- Lane 01 4d225d1: missing hardlane root half (29 files): BigTreasure assets, motion/staging/installer/arena; BombSarai installer/arena; Fuefuki installer/arena; disc-parameter reader and supporting tests/audits. These accompany the previously integrated native modules. BigTreasure's full staging requirement remains 29 converted clips; bringing in tooling does not establish conversion completion.
- Lane 31 e64d3c5: BlackMan/Tyre visual staging profile and tests. The native visual consumer and ordinary Waterwraith actor remain future work. Resolved documentation-only context conflict by retaining worker evidence, explicitly separate from this sweep's validation.
- Lane 19 1612046: corrected worker Mamuta stability evidence to 8/8 committed-fixture runs, retaining its pinned executable provenance.
- Integration guards: reject path components in BigTreasure staged filenames; preflight Waterwraith species and pose filenames before writes. Regression rejects a ../ pose path without creating the staging output.
- New arena helpers use the current preview overlay (default live 20-red squad). Native centred 960x540 baseline is unchanged. No assets or runtime directories committed.

## Reviewed queue

- Lane 32 fa95dd7 rebases the existing FSM-host series. Compared native d288dd7b FSM host and runtime tool files with our native: identical, so no duplicate application.
- Crawbster 1ad3669 depends on source host pc_p2_dangomushi.cpp from the pending species branch. Its own provenance says vulnerability is observed rather than applied, and Rock/Egg outputs remain decisions rather than births. Policy remains integrated; host bundle queued.
- Bulbmin cd839ab, captain 37ea61b and elemental 19ee57e are new shared actor/receiver adapter handoffs. They remain queued with lane-11 identity/schema dependencies for semantic reconciliation.
- QA 37a932d re-pins worker evidence to the lane-01 candidate pair, not this draft. Retained this draft's exact provenance rather than replacing its baseline with a worker pin.
- Broad species, shared lifecycle/provider, normal combat/reward delivery and AP/CLI/native P2 eligibility remain open. Added reference/staging tools do not establish an all-P2 playable pool.

## Validation

Full suite: **2020 passed, 25 skipped, 1088 subtests passed** in 185.48s. Focused new-tool tests: 71 passed, 1 skipped; includes 9 Waterwraith tests and path-escape rejection. No native edits or rebuild; #451 production build and source export provenance remain applicable. No new real-GL acceptance run.
