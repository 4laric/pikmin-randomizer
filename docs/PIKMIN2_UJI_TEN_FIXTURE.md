# Ten-actor Beasts developer fixture

`python -m experimental.pikmin2_uji_ten_fixture --native native --recipe <current verified Ninja commands.json> --output <new private directory>`

This new fixture reuses the private Uji fixture builder while substituting a ten-actor driver. All generated sources, objects, executable and import library stay private. Link only after the coordinating native build is stable. It does not modify production code or existing staging modules.

The driver requires exact IDs61000–61009, four UjiB followed by six UjiA and source corpse values2/1. It leaves actors at their staged source-candidate positions, captures the live scene, forces health to zero and invokes the existing native corpse-conversion boundary, then walks the captain through the connector and assigns two native carriers to each corpse. It observes every corpse west of the connector, requires ten distinct receipts totaling14, duplicate-economy replay0 inside valid delivery callbacks, unchanged repairs and source treasure value100 accounted separately if followers collect it opportunistically. No corpse relocation is performed.

Direct corpse conversion (bypassing death-animation completion) and assigned tasks are developer controls, not natural player combat or full P2 AI acceptance. Static source poses and P1 proxy behaviors remain as documented. The initial live capture is a scene capture from the start room; independent close-up visual QA remains separate. Two focused tests validate source framing and exact receipt/crossing evidence.

Private fixture04 passed against native156cbefa:10 registrations,10 observed west-connector crossings, ten distinct corpse receipts totaling14, duplicate checks0 and unchanged repairs. Source100-Poko cargo remained uncollected in the successful run. Evidence: output/p2-uji-ten-fixture-batch/runs/0a2d8355e925477e9d26a26e4c3d056a/acceptance.json. Direct native corpse conversion was necessary to isolate transport from buried/offscreen death-animation progress; the latter remains unvalidated.
