# Male bite/eat visual behavior fixture

This bounded source-backed stimulus retains one source UjiB generator (61000) at its authored Beasts position and one prey Pikmin at an explicit prelaunch engineering position (1055,0,-100). Other Uji actors/prey are removed only from the private scene. The driver assigns the prey a normal Transport task toward the existing heavy juji cargo. A captain controller walks across the connector into view and then retreats. No enemy motion/state/health writes, direct attack or direct swallow calls are used.

Native KabekuiB strategy selects visible targets, approaches them, enters BiteAttack(state6/Type1), and enters BiteChewing(state7/Type2) only after mouth capture. TAIAattack.cpp:368-434 drives bite-switch windows from native motion events and InteractSwallow on mouth-sphere contact. TAIAeatPiki at447-455 kills mouth-attached Pikmin at the native action event. The fixture observes states6/7, actual mouth pointer, then prey death. Animated visual evidence must show changing attack2 and eat poses; static control must retain static rendering. Economy must remain zeroPokos and repairs unchanged.

The female bridge gate is explicitly unsupported in this scene. TAIAsearchWorkObject (TAIAjudge.cpp:31-55) requires an actual WorkObject bridge within danger territory and getFirstFinishedStage()>=0. The assembled Beasts scene contains no registered built bridge. A static mesh or cave connector is not a valid replacement. Native bridge destruction/attack1 visuals require a later original P1 bridge scene or a properly staged WorkObject and legitimate construction.

Run `experimental.pikmin2_uji_bite_fixture build` with --native --build-dir --output --head, using a coordinated fresh native build. It reuses the private instrumented-family builder without changing maintained source. `run` takes --assets --assembly --pod --content --uji --output --exe --bank. Each static/animated case is isolated and emits bite-acceptance.json only on complete native and visual evidence. Two focused tests pass; runtime pending.


The initial free-prey stimulus failed identically with static and animated rendering: the prey attacked and killed the male before capture. These are preserved at output/p2-uji-bite-runtime/validation01/runs/9cc9bc8ee3e24ecfa2fb8166a845705f (static) and a5b685637fdb4b4ebb4f6d9dc8090032 (animated); this is not an animation-defect claim. The approved transport-bait refinement changes the injected Pikmin task, not enemy behavior. TAIAvisiblePiki permits carrying prey (only rejects attached-to-this-enemy); recognition requires visible/alive/nonburied. InteractSwallow abandons the current action and releases non-mouth attachment before mouth capture. No direct swallow is invoked by the fixture. Captain retreat avoids remaining a preferred nearby target. One bounded static/animated rerun is pending.


Transport-bait geometry audit: the source male remains at(1055,0,-170), while the occupied carrier attaches around(1039,0,6), about177 units away. KabekuiB VisibleRange is150 (TAIkabekuiB.cpp:211), with360-degree angle and100 height. The captain triggers appearance but its retreat lets the male burrow/return to Waiting without seeing the carrier. This is a known fixture layout limitation, not proof of a bite/eat or animation bug. A later bait cargo placement must be explicitly engineered within the verified perception radius while preserving the source enemy location. No further unreviewed stimulus variants are included.


Approved near-bait scene: cargo center(1055,0,-120), prey start(1055,0,-100), unchanged source male(1055,0,-170). All ten predicted carry slots and both centers pass imported-terrain height checks. The manifest records explicit engineering transforms and generator before/after hashes. Before assigning Transport, the native fixture evaluates actual getSlotGlobalPos for every slot: all are30..70units from the male, within80attack/150vision. No runtime relocation occurs.

Both static and animated native behavior PASSED with normal BiteAttack/BiteChewing, actual mouth pointer, prey death, zeroPokos and unchanged repairs. Static acceptance is output/p2-uji-bite-runtime/validation03-near-bait/runs/40c2b6bf27f349e3b7936bd7aedd50f1/bite-acceptance.json. Animated run9e369af5c8fe4416b8f1d5a1d527ebda has native behavior PASS but visual acceptance FAIL: render logs cover appear/move, not attack2/eat. Captain retreat makes off-camera culling the likely observation gap; do not claim bite/eat visual sign-off. Its bite-diagnostic.json records this distinction. A fixed observer-camera fixture is the proposed next step, preserving captain behavior. Female bridge remains unsupported.


Observer-camera refinement is prepared: private PcamCamera::setTarget(male) uses the engine's supported non-Navi Creature watchpoint, preserving default FOV/camera motion and the captain route. Camera position/focus/FOV/frustum visibility and state6/7 captures are recorded. Its fixture04-observer private compile/link completed but final native freshness rejected pending Ninja work. That executable was not run. A newly completed stable native build is required before visual acceptance resumes.
