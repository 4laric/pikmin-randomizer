# Species expansion integration #456

Owner: Codex through shared account 4laric. User confirmed species push 3efcd4b on kimi/p2-bulblax-import (lane01-reconcile itself remained at 4d225d1). Draft baseline 3120600 / native fb7fee46. Native result f14c6851473ac1161be56c8b98f4f905232f3635.

## Integrated scope

- Root 3efcd4b: Jellyfloat expansion evidence and mixed-scene harness/tests. Historical family-status promotion hunks were not imported as current acceptance claims.
- Extracted Jellyfloat-only native delta 4597eaee^1..8641da60: Lesser/Greater policy, mouth, flight/FSM, visual/arena and opt-in Teki bindings; ingestion controller and fixtures. Source commit 23fb74e0, integration correction f14c6851.
- Added current-line CMake, final-setup, Teki tick/draw/reset/forget and stage-teardown hooks. Preserved current Giant Breadbug, projectile, Snow/Kogane and other family hooks. Did not pull the entire older species tree or unrelated whistle-pluck commit.
- Receiver migration retains current snapshot-before-callback reset/release, reentrant-registration checks, externally detached scale restoration, generation checks and Piki/owner predeath invalidation. The new ingestion controller is composed into those existing guards. Receiver ownership/digestion engine-double regressions pass.
- Fixed worker bindings retaining pointers to stack-local mouth/suck collision parts: register bindings first, then pass addresses of stable map-owned parts. Fixed the capture-slot accessor's 2-vs-10 array mismatch found by the combined build.

## Evidence and limits

Worker 8641da60 reports Greater auto-binding and Lesser ingestion fixture passes with 20 reds and centred 960x540 windows. These results are pinned to the worker executable, not this combined binary. No new real-GL run in this integration.

Mixed-scene worker baseline is 12 species + one control: mean 33.45 ms and slowest-window mean 33.93 ms, versus a proposed 16.7 ms mean target. The proposed target is not met. Texture peak 64 MiB and bank bytes 4,883,616 are separate measurements. Proposed budgets are not accepted production limits; harness correctness PASS is separate from its budget assessment.

The mixed harness is integrated, but its full species native dependency chain is not. Ground/flying/aquatic/snags require per-family reconciliation. Do not run this draft and claim the worker's 12 source FSMs are all present.

Jellyfloat bindings remain opt-in/private-adapter paths. Source flight policies and arena clock are not proof of complete ordinary host FSM execution. Shared receiver has one owner context; simultaneous Lesser/Greater capture is not accepted. Native captain capture/release, moving joint fidelity, materials, ordinary enemy damage/rewards, restart and generated-seed admission remain open. Current source has no invincibility-query bridge for ingestion admission; it retains existing host admission guards. No family-complete or randomizer-eligible promotion.

## Validation

Production build PASS at f14c6851473ac1161be56c8b98f4f905232f3635, no-work dry run. Executable SHA-256 `CFF99C97302A03080CE02DDCCF644794E1DD1954516949BE1FC4967EBEC4796F`. Exact export parity: all 1724 source files. Native standalone gate: 31/31 PASS. Full Python suite: 2029 passed, 26 skipped, 1088 subtests passed in 275.79 seconds. Private build output/p2-upstream433-build, Ninja Release/MinGW, JAudio ON, test hooks OFF. First build rejected capture-slot accessor type mismatch; corrected and rebuilt. Focused receiver + mixed-scene tests: 9 passed, 1 skipped, 2 subtests passed.
