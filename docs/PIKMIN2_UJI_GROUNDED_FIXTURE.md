# Grounded Sheargrub acceptance

This private fixture validates the corrected ten-Uji Hole of Beasts profile: four UjiB and six UjiA, native IDs 61000-61009. It never assigns health zero or directly converts enemies to corpses. Buried actors must reject injected attacks; the captain walks normal controller waypoints, then vulnerable actors receive legitimate InteractAttack events. Native death must produce ten corpses. Twenty Pikmin receive transport actions, with native movement and attachment across the connector. Source corpse credits must total 14 Pokos; juji_key_fc cargo remains separately worth 100, with repairs unchanged. This is injected developer input, not manual combat or full P2 AI; actors retain documented P1 proxy AI and P2 static pose visuals.

Use `experimental.pikmin2_uji_grounded_fixture build` with --native, --build, --output and --expected-head. It snapshots a completed native build privately. `run` takes --assets, --assembly, --pod, --content, --uji, --output and --exe. Each run is isolated; acceptance.json appears only after every gate passes.

Fixture01 was rejected by its final build freshness guard and never run. Fixture03 built cleanly against e704b268. Initial diagnostics proved generator centers matched the manifest, but inherited GenAreaCircle radius50 scattered births. Source generator.cpp:1383 samples this radius. The approved private override records fixture-radius-override.json with before/after hashes, selected IDs and radius50-to-zero engineering policy. It validates the exact circle block and changes only the radius float; production staging is unchanged. Three focused tests cover this boundary and failure preservation.

The radius-zero run exposed normal first-tick collision drift of up to 5.4 units for two grouped females. Exact XYZ acceptance therefore reads the native stored birth position Teki::mPersonality->mPosition and generator center; current coordinates and drift are logged separately. GenObjectTeki::birth copies BirthInfo position into the field before reset/startAI. BTeki::reset passes it to resetPosition. The only other write in plugPikiNakata is BTeki::generateTeki creating a separate actor, not these registered waiting actors. This asserts birth position, not immobility after physics. Source yaw remains unapplied.

Runtime acceptance PASSED with fixture04 against native e704b268: ten exact stored birth positions/generator centers, ten rejected buried attacks, ten legitimate vulnerable attacks, ten native corpses, ten connector crossings, ten unique source receipts totaling 14 Pokos, and unchanged repairs. The 100-Poko source treasure remains separate. Evidence is output/p2-uji-grounded/validation04/runs/b9d691b8206b4fd3a419c806d3e60f11/{native.log,acceptance.json,fixture-radius-override.json}; private build provenance is output/p2-uji-grounded/fixture04/linked/provenance.json. Diagnostic failures in validation02/03 remain retained. This does not validate manual combat, P2 AI fidelity, source yaw, or production randomized spawn scatter. Three focused tests pass.
